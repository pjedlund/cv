<footer class="footer">
    <div class="container utl-space-between">
        <p>&copy; {{ author.name }}</p>
        <p>{{ strings.labels.lastUpdated }}: {{ build.timestamp | dateToFormat('yyyy-MM-dd') }}</p>
        {% if build.demo %}
            <a href="https://github.com/maxboeck/resume" class="gh-link utl-screen-only">
                <span>{% icon "github" %} view source</span>
            </a>
        {% endif%}
    </div>
</footer>
